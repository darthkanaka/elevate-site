# Elevate Media HI

The custom rebuild of elevatemediahi.com, replacing the Squarespace site.
Live at https://www.elevatemediahi.com since 2026-09-23.

Static HTML, CSS and JavaScript. No build step, no bundler, no package manager.
Edit a file, commit, push. GitHub Pages serves `main` from the repo root, and the
change is live in about a minute. `CNAME` holds www.elevatemediahi.com; do not delete it.

## Layout

```
index.html          homepage
portfolio.html      generated by tools/build-portfolio.py, do not hand edit the rows
contact.html        contact form
terms.html          privacy policy and SMS terms
CNAME               custom domain for GitHub Pages
assets/css/site.css one stylesheet, sectioned and numbered
assets/js/site.js   one IIFE per effect, each independently deletable
assets/fonts/       Poppins 300 to 700, latin subset, self hosted
assets/img/         web sized images
assets/video/       hero loop and the two service loops
gas/                source of the contact form's Apps Script web app
tools/              portfolio generator plus the Playwright and axe checks
harvest/            the old Squarespace site, archived. Read harvest/manifest.md
```

## Local preview

```
python3 -m http.server 8777
```

Then open http://localhost:8777.

## Contact form

Both forms (homepage and contact page) post to a Google Apps Script web app
named "Elevate Media contact form", owned by kawika@elevatemediahi.com. The URL
is in each form's `data-endpoint` attribute. The script logs every enquiry to a
Google Sheet, then emails kawika@elevatemediahi.com with Reply-To set to the
sender. Its source is `gas/contact-notify.gs`.

To change the script: paste the new source into the Apps Script editor, then
Deploy, Manage deployments, edit the existing deployment, Version: New version.
Never use New deployment. That mints a new URL and the site keeps posting to
the old one.

Spam guard is a hidden honeypot field plus a 3 second minimum fill time. If the
post fails, the visitor is shown a mailto link instead.

## Phone number

The public number is 808 204 4575 (`tel:+18082044575`), a Twilio line that
forwards to Kawika's cell. The cell itself is never published. The old number
survives only inside `harvest/`.

## Portfolio

Put images in `assets/img/portfolio/`, edit the project list in
`tools/build-portfolio.py`, and run it. See `tools/README.md` for how the rows
are packed so nothing gets cropped.

## Checks

```
cd tools && npm install
BASE=https://www.elevatemediahi.com node verify.mjs
BASE=https://www.elevatemediahi.com node a11y.mjs
```

`verify.mjs` looks for console errors, failed requests, missing alt text, bare
`#` links, unlabeled fields, a leftover noindex, and horizontal overflow on
every page. `a11y.mjs` runs axe-core at WCAG 2.1 AA plus best practice. Both
default to the local preview on port 8777 when `BASE` is not set.

## DNS

The domain is registered at Squarespace Domains until 2027-07-12, and DNS is
managed there. Four A records on @ point at 185.199.108.153, .109, .110 and
.111; www is a CNAME to darthkanaka.github.io. Never touch the MX records, they
carry Google Workspace mail.

## Rules this codebase keeps

These are not preferences. Each one is a defect found in the reference build
this site borrowed its effects from, and the fix is structural rather than a
patch.

1. Nothing animates when the visitor has asked for reduced motion. The script
   checks once at the top and every effect respects it, and the stylesheet
   kills anything the script might miss.
2. No animation loop runs while its section is off screen or the tab is
   hidden. Every canvas and marquee goes through one `visibleLoop` helper.
3. Content is never hidden by default. A `js` class on the root element, set by
   one inline line, is what enables the hidden-until-revealed states, so with
   JavaScript off the page is complete rather than blank.
4. Canvases scale by device pixel ratio. Pointer listeners are passive. Resize
   handlers are debounced.
5. The pinned horizontal rail is desktop and fine-pointer only. On touch the
   same markup is a native scroll-snap row.
6. Every image carries alt text, every field has a real label, focus is always
   visible, and text clears 4.5 to 1 contrast.
7. No em dashes anywhere, in copy or in comments.

## Verified

Checked with Playwright and axe-core across the homepage, contact and terms, at
1440, 1024 and 390 pixels:

- No console errors, no failed requests, no horizontal overflow.
- Zero accessibility violations under WCAG 2.1 AA plus best practice.
- With JavaScript disabled: all content visible, nothing stuck invisible.
- With reduced motion: particle canvas removed, marquee stopped, band
  un-rotated, nothing left hidden.
- On mobile: hamburger opens and closes, escape closes it, the work rail is a
  scroll row rather than a pinned section.
